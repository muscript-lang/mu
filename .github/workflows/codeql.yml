name: codeql

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "21 5 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (Rust)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Initialize CodeQL
        uses: github/codeql-action/init@f72b527f02f6081c2db4065f63ad7db7d40e2f65
        with:
          languages: rust

      - name: Autobuild
        uses: github/codeql-action/autobuild@f72b527f02f6081c2db4065f63ad7db7d40e2f65

      - name: Analyze
        uses: github/codeql-action/analyze@f72b527f02f6081c2db4065f63ad7db7d40e2f65
